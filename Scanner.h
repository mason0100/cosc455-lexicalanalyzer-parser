#pragma once

#include "Automaton.h"
#include"Token.h"
#include <fstream>
#include <iostream>
#include <string>
using namespace std;

class Scanner{

	////Scanner scans the file line by line extreacting tokens as the pointers progress through the line. 
	//Automaton automata;
	//Token token;


	////creates file object
	//fstream myfile;

	////opens file
	//myfile.open("example.txt");

	////string variable for reading file 
	//string str;


	////read file code for the future
	////This while statement reads a .txt file line by line
	//while (getline(myfile, str)) {
	//	for (int i = 0; i < str.length(); i++) {
	//		cout << str[i] << endl;
	//	}
	//	cout << str << endl;
	//}

	//myfile.close();


};//end Scanner class

